<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- 字体大小 -->
    <group android:checkableBehavior="single">
        <item android:title="小" android:id="@+id/font_small" app:showAsAction="never"/>
        <item android:title="中" android:id="@+id/font_medium" app:showAsAction="never"/>
        <item android:title="大" android:id="@+id/font_large" app:showAsAction="never"/>
    </group>

    <!-- 字体颜色 -->
    <group android:checkableBehavior="single">
        <item android:title="红色" android:id="@+id/color_red" app:showAsAction="never"/>
        <item android:title="黑色" android:id="@+id/color_black" app:showAsAction="never"/>
    </group>

    <!-- 普通菜单项 -->
    <item android:title="普通菜单项" android:id="@+id/menu_normal" app:showAsAction="never"/>

</menu>